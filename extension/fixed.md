# Fixed

- Duplicate WebSockets on concurrent `ensureConnected` (shared in-flight connect)
- `tabs.onUpdated` wakes worker for discarded events (native `properties` filter)
- Bookmark overlay prefetch reads untouched fields (scoped prefetch by burst kind)
- Background title tickers pin worker awake (timerless stash + piggyback drain)
- Sustained churn holds ~6 sync cycles/min (adaptive empty-cycle backoff)
- Reconnect backoff waits 2x too long; long resumes pin worker (compute-once delay + resume gate)
- WS `changes_available` runs a full sync per push (debounced via `scheduleLocalSync`)
- HistoryVisit-only churn resets the throttle streak (significant-ops backoff)
- Trailing local-sync timer pins worker ~60s in deep backoff (30s hold cap)
- Reconnect retries fire in lockstep after shared outages (timer jitter)
- Alarm does full sync work on never-registered installs (`getDevice` gate)
- Active-tab title tickers re-arm the debounce timer forever (rearm downgrade + stash cap)
- `toB64` spread pays iterator overhead per byte (`.apply` indexed access)
- History live capture nudges a full sync per visit burst (skip nudge, ride alarm like activate ops)
- Chained catch-up has no wall-clock bound (60s chain budget via `shouldChainAnotherCycle`)
- Idle tick re-fetches settings over HTTPS to re-learn an unchanged policy (30-min `fetchTabRestorePolicy` cache)
- Idle tick polls download with zero changes despite a healthy push channel (bounded empty-poll skip via `notifyPeerChanges`)
- Switch storms sync one activate op per visited tab (per-window last-activation coalescing)
- Bookmark move batches re-sort siblings on every move (sort-once + binary-search insert)
- Disabled sync types still captured, encrypted, and uploaded (sync listener kill-switches + flush-time gates, fail-open)
- History backfill imports ad/iframe visits as user history (`auto_subframe` transition filter, backfill-only)
- Chronic title tickers stash forever and mint a trailing op per episode (drop ticks past the rearm downgrade)
- Popup walks 60 history rows to display 5 (bounded `getSyncedHistoryVisits(5)`, 75% fewer rows per open)
- Local burst right after an alarm fires a duplicate cycle (every `runSyncCycle` feeds `lastLocalSyncFire`, nudge respects min-interval)
- SW serves stale device/settings until restart after popup register/save/disconnect (memory-only invalidators + `storage.session.onChanged` listeners, wired into `REFRESH_CAPTURE_CONFIG`/`DEVICE_DISCONNECTED`)
